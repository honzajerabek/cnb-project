## CNB APP


1. install deps
    ```bash
    yarn
    ```

    (ios only) install pods
    ```bash
   cd ios && pod install
   ```

2. run locally using
    ```bash
    yarn ios
    ```
    or
    ```bash
    yarn android
    ```
   

## Intl
App supports English and Czech languages, determined by the system settings.

